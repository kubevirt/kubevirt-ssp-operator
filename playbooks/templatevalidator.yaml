- hosts: localhost
  gather_facts: no
  tasks:
  - name: Gather informations about the repository
    import_role:
      name: "KubevirtRepoInfo"
  - name: Deploy the Template Validator
    import_role:
      name: "KubevirtTemplateValidator"
